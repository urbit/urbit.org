::  Lesson 04: splat routing and request path fields
::
::  Install path: /learn/04-rest-routing
::  Try:          /o/learn/04-rest-routing/alpha/beta
::
::  New idea:
::    A runtime endpoint is a prefix route. The endpoint serves its own URL
::    and deeper paths below it.
::
|=  req=http-request
=/  m  (strand ,~)
^-  form:m
=*  r  ~(. server req ~)
%-  respond-html:r
;html
  ;head
    ;meta(charset "UTF-8");
    ;meta(name "viewport", content "width=device-width, initial-scale=1");
    ;link(rel "stylesheet", href "/hawk499/assets/feather");
    ;title: Lesson 04 — routing
  ==
  ;body.page.prose
    ;h1: Lesson 04 — splat routing
    ;p
      ; Open this endpoint with extra path segments, such as
      ;code: /o/learn/04-rest-routing/alpha/beta
    ==
    ;dl
      ;dt: prefix.req
      ;dd.mono: {(spud prefix.req)}
      ;dt: rest.req
      ;dd.mono: {(spud rest.req)}
      ;dt: base.req
      ;dd.mono: {(spud base.req)}
      ;dt: raw url.req
      ;dd.mono: {(trip url.req)}
    ==
    ;p
      ; In beginner terms: prefix.req is the route that matched; rest.req
      ; is what was left over below that route.
    ==
  ==
==
